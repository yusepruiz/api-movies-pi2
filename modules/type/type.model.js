/*** Tipos de películas ***/

/**
 * Crea un nuevo tipo de película en la base de datos
 * @param {string} name 
 * @param {string} description 
 */
export const createType = (name, description) => {
    try {
        const query = "";

    } catch (error) {
        console.error("Error al crear el tipo de película");
    }
}
